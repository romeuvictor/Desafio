 {{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}} 

 {{/each}}

 
  <h3>Alterar dados do Cliente</h3>
    <div class="card">
        <div class="card-body">
          <form action="/admin/clientes/editar" method="POST">
                <imput type="hidden" nome="id" value={{Clientes._id}}>
                <label for="nome"> Nome: </label>
                    <input type="text" id="nome" name="nome" class="form-control " value ="{{Clientes.nome}}" />
               <label for="idade"> Idade: </label>
                    <input type="number" id="idade" name="idade"class="form-control"    value ="{{Clientes.idade}}"/>
                <label for="endereco"> Endereço: </label>
                    <input type="text" id="endereco" name="endereco" class="form-control" value = "{{Clientes.endereco}}"/>
                <label for="conta"> Conta: </label>
                    <input type="number" id="conta" name="conta" placeholder="Numero da Conta"class="form-control" value ="{{Clientes.conta}}" />
                <br>
                <button type="submit" class="btn btn-success">Alterar dados</button>
          </form>
      </div>
  </div>